name: Readme Tree

on:
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  contents: write
  pull-requests: write

jobs:
  readme-tree:
    uses: devantler-tech/workflows/meta/readme-tree.yaml@main
    secrets: inherit
