# Inference routing

Read the repository-root `AGENTS.md` **Inference routing** section before assigning a model or
delegating. For this temporary Antigravity surface, follow the canonical adapter at
`.claude/loaders/antigravity-observer.md`. A disabled or expired registration does not authorize
dispatch. This rule is a pointer; policy and runtime permissions are defined by those sources.
